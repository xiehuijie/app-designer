{
    "$id": "app-designer/types/base.schema.json",
    "title": "BaseTypeDefinitions",
    "definitions": {
        "Text": {
            "type": "object",
            "title": "多语言文本描述",
            "propertyNames": {
                "type": "string",
                "description": "语言代码，例如`en`、`zh`等。",
                "examples": [
                    "en",
                    "zh",
                    "ja"
                ]
            },
            "additionalProperties": {
                "type": "string",
                "description": "对应语言的具体文案描述。"
            }
        }
    },
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "title": "类型唯一标识符",
            "description": "类型的唯一标识符，在应用程序中必须唯一。"
        },
        "title": {
            "$ref": "#/definitions/Text"
        },
        "description": {
            "$ref": "#/definitions/Text"
        },
        "icon": {
            "type": "string",
            "default": ""
        },
        "sort": {
            "type": "number"
        },
        "type": {
            "type": "string"
        },
        "examples": {
            "type": "array"
        }
    },
    "required": [
        "id",
        "title",
        "sort",
        "type",
        "examples"
    ]
}