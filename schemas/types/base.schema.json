{
    "$id": "app-designer/types/base.schema.json",
    "title": "BaseTypeDefinitions",
    "type": "object",
    "properties": {
        "id": {
            "type": "string",
            "title": "类型唯一标识符",
            "description": "类型的唯一标识符，在应用程序中必须唯一。"
        },
        "title": {
            "$ref": "../common/i18nText.schema.json"
        },
        "description": {
            "$ref": "../common/i18nText.schema.json"
        },
        "icon": {
            "type": "string",
            "title": "图标",
            "description": "类型的图标，基于iconify库的图标名称，通常以图标库前缀再加图标名称的形式表示，例如：mdi:account。iconify参考地址：https://icones.js.org"
        },
        "sort": {
            "type": "number"
        },
        "type": {
            "type": "string"
        },
        "examples": {
            "type": "array",
            "default": []
        }
    },
    "required": [
        "id",
        "type"
    ]
}