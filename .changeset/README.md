# Changesets

This folder stores release intent files generated by `pnpm changeset`.

Workflow summary:

1. Add user-facing changes in `apps/*` or `packages/*`.
2. Run `pnpm changeset` and commit the generated markdown file.
3. After merge to `main`, CI creates/updates a version PR.
4. Merge the version PR to apply version bumps and changelog updates.
